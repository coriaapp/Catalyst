"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.crypto = void 0;
var win = (typeof self === 'object' && self.self === self && self) ||
    (typeof global === 'object' && global.global === global && global) ||
    this;
exports.crypto = win.crypto;
if (!exports.crypto) {
    // Node versions <19.0.0 need a special flag to enable the global `crypto` object.
    // We use `eval` so that webpack doesn't try to bundle the `crypto` module.
    // eslint-disable-next-line no-eval
    exports.crypto = eval('require("crypto").webcrypto');
}
//# sourceMappingURL=crypto.js.map